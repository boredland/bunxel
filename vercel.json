{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "curl -fsSL https://bun.sh/install | bash && ~/.bun/bin/bun install --frozen-lockfile --yarn --cache-dir $(yarn cache dir)/bun --verbose",
  "buildCommand": "~/.bun/bin/bun run build"
}
